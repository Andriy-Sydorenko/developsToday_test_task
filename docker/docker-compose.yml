services:
  api:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    env_file:
      - ../.env
    ports:
      - "${PORT:-8000}:${PORT:-8000}"
    volumes:
      - api_data:/app/data

volumes:
  api_data:

